<?php

$num = floatval(readline());
$counter = 0;
$min = PHP_INT_MAX;
while ($counter < $num) 
{
    $newNum = intval(readline());
    if ($min > $newNum)
    {
        $min = $newNum;
    }
    $counter ++;
}
echo $min;





